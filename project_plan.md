# 프로젝트 계획서

## 1. 프로젝트 주제

내가 만들 프로그램의 주제: 비밀번호 안전성 평가와 비밀번호 추천 프로그램


---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.

내 설명: 최근 여러 해킹 공격에 의해 우리의 개인정보나 계정이 탈취되거나 그런 시도가 일어나는 경우가 자주 발생한다(저도 이라크 쪽 에서 네이버 계정을 탈취할 뻔했다고 메일이 온 적이 있습니다.) 그리고 최근에는 귀찮음을 이유로 비밀번호를 복잡히 하지 않고 반복적인 단어를 쓰거나 짧게 하는 등 안일한 점이 존재한다. 이러한 문제에 대한 경각심을 주고 더 안전한 정보보안을 위하여 비밀번호의 안전성의 결여 등을 경고해 사용자들의 보안의식의 부족을 일깨우고 더 안전한 비밀번호를 쓰게 하는 것이 목표다.  


---

## 3. 입력-처리-출력 구조

### 입력



- 비밀번호(문자열) 입력.
- 평가 결과 출력 이후에는 두가지로 나눈다. 1번은 비밀번호 위험의 이유를 알고 싶은지에 대한 답을 Y/N 식으로 입력한다.
- 같은 방법으로 추천 번호를 알고싶냐는 질문에 답을 한다.

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

- 1단계: 공백 유효성 검사 및 프로그램 제어 알고리즘

사용자가 입력한 비밀번호 문자열을 for문으로 한 글자씩 순회하며 공백 문자(" ")가 포함되어 있는지 if문으로 검사합니다.

공백이 발견되면 상태 변수(has_space)를 1로 변경하고 break로 루프를 탈출한 뒤, 뒤이어 나오는 안전성 진단 함수를 실행하지 않고 오류 메시지와 함께 프로그램을 즉시 안전하게 종료(예외 처리)합니다.
- 2단계: 2차원 리스트 순회 및 규칙별 보안성 평가 알고리즘

공백이 없는 유효한 입력일 경우, 사전에 정의한 2차원 리스트(password_criteria)를 for문으로 순회하며 각 행에 저장된 규칙명, 기준 점수, 설명 데이터를 차례대로 추출합니다.

추출한 규칙명에 따라 다중 조건문(if-elif)으로 분기하여 문자열 길이 검사(len() 활용)와 숫자 포함 개수 누적 검사(in 연산자 활용)를 수행하고, 조건 만족 시 누적 총점(total_score)에 가산점을 더합니다. 조건 미달 시 실패 원인 리스트(failed_reasons)에 해당 사유를 기록합니다.
- 3단계: 인덱싱 기반 3연속 동일 문자 판별 및 등급 산출 알고리즘

감점 규칙인 반복 문자 검사 시, 문자열 인덱스 범위를 초과하는 에러(IndexError)를 방지하기 위해 range(len(password) - 2) 범위만큼만 반복하며 and 연산자로 현재 글자와 다음 글자들을 연속 비교합니다.

3연속 동일 문자가 적발되면 상태 변수를 1로 바꾸고 음수 점수(-3)를 총점에 더해 자동 감점 처리를 합니다. 모든 검사가 끝나면 최종 누적 점수를 바탕으로 조건문을 통해 3단계 안전 등급(안전/보통/위험)을 판별하여 출력합니다.

### 출력

크게 세가지로 나눈다. 

- 비밀번호 안전성을 평가한 후 처음에는 위험 점수와 위험성 여부 평가 결과를 출력한다.
- 문제 요소를 알고 싶다는 답이 오면, 감점 원인을 출력한다.
- 비밀번호 추천을 원한다는 답에는, 수정된 비밀번호를 표시한다.

---

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름: password_criteria



```
# password_criteria = [
    ["length", -2, "비밀번호 길이가 8자리 이상인가?"],
    ["number", 2, "숫자가 포함되어 있는가?"],
    ["repetition", -3, "동일한 문자가 3번 연속 반복되는가? (감점)"]
]
```

### 한 행의 의미


### 각 열의 의미

| 열 번호 | 의미 | 예시 |
|---:|---|---|
| 0번 열 | 규칙을 구분하고 조건문에서 판별하기 위한 규칙 이름 (식별자) | "length", "number" , "repetition"|
| 1번 열 | 해당 조건을 만족했을 때 부여하거나 차감할 기준 점수 (가산점/감점) | 3, 2, -3 |
| 2번 열 | 사용자 화면에 검사 항목으로 출력할 규칙에 대한 설명 메시지 | 비밀번호 길이가 8자리 이상인가? |
| 3번 열 | (선택 사항) 해당 규칙을 통과하지 못하면 무조건 '위험'으로 처리할지 결정하는 필수 여부 (True/False) | "TRUE", "FALSE" |

---

## 5. 함수 설계

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| get_password | 사용자에게 주의사항(개인정보 보호 등)을 안내하고, 안전하게 비밀번호를 입력받습니다. | 없음 | 사용자가 입력한 비밀번호 (문자열) |
| check_length | 입력된 비밀번호의 길이가 8자 이상인지 판단한다. | password (문자열) | 만족 여부 (True/False) |
| evaluate_password | 2차원 리스트 규칙을 돌며 전체 점수를 계산하고 합산한다. | password (문자열), criteria (2차원 리스트) | 최종 계산된 총점 (정수) |
| print_result | 최종 점수에 따라 안전 등급을 판정하고 결과를 화면에 예쁘게 보여준다. | total_score (정수) | 없음 (화면에 결과 바로 출력) |

---

## 6. 사용할 제어구조

### 조건문을 사용할 부분


내 계획: 글자수 일정 기준보다 작을때 마다, 작은 수 만큼 감점한다.
         숫자가 포함되면 일정 점수를 추가한다
         반복이 보이면 감점한다.

### 반복문을 사용할 부분

 

내 계획: 2차원 리스트의 모든 항목을 하나씩 검사해 점수를 측계한다.


---

## 7. 예상되는 예외 상황

예) 조건에 맞는 추천 결과가 없을 때, 잘못된 입력을 했을 때 등

1. 아예 아무것도 안 쓴 경우
2. 비밀번호 작성 중, 공백을 사용했을 경우

---

## 8. Gemini에게 도움받고 싶은 부분

예) 주제 구체화, 2차원 리스트 구조, 함수 분리, 오류 수정 등

1. 2차원 리스트를 어디서 활용할지 묻기
2. 함수 분리
3. 평가 조건 만족 여부 판단
