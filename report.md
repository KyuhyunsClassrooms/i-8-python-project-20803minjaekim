# 최종 보고서

## 1. 프로그램 주제와 목적

내 프로그램의 주제: 비밀번호 안전성 평가와 추천 프로그램

이 프로그램을 만든 이유: 최근  이라크 쪽 에서 네이버 계정을 탈취할 뻔했다고 메일이 온 적이 있어, 보안에 대한 중요성을 인지했습니다. 그리고 최근에는 귀찮음을 이유로 비밀번호를 복잡히 하지 않고 반복적인 단어를 쓰거나 짧게 하는 등 안일한 점이 존재합니다. 이러한 문제에 대한 경각심을 주고 더 안전한 정보보안을 위하여 비밀번호의 안전성의 결여 등을 경고해 사용자들의 보안의식의 부족을 일깨우고 더 안전한 비밀번호를 쓰게 하는 것이 목표입니다.


---

## 2. 문제 분해 및 모델링

### 입력: 
가상의 비밀번호 (문자열)


### 처리 :
예외 처리: for문과 if문을 활용해 비밀번호 내부에 공백(" ")이 존재하는지 검사한다.

규칙 순회: 공백이 없을 때, 2차원 리스트를 for문으로 돌며 각 보안 규칙 정보를 추출합니다

조건 검사: if-elif 구문을 통해 문자열의 길이 검사, 숫자 개수 누적, and 연산자를 활용한 3연속 동일 문자 판별 수행합니다

점수 계산: 규칙 만족 여부에 따라 가산점을 더하거나 감점 점수를 총점에 합산해 결과를 발표합니다


### 출력 :
공백 발견 시: 유효하지 않은 입력이라는 오류 메시지 출력 후 프로그램 종료

정상 입력 시: 항목별 검사 결과(성공/실패/위험), 최종 안전성 점수와 안전 등급(안전/보통/위험) 출력

선택 출력: 사용자의 동의(Y) 하에 기존 비밀번호를 보완한 맞춤형 추천 비밀번호 출력


---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름: password_criteria

한 행의 의미: 비밀번호의 안전성을 평가하기 위한 개별적인 '하나의 보안 검사 규칙'

각 열의 의미:

| 열 번호 | 의미 | 실제 예시 |
|---:|---|---|
| 0번 열 | 규칙을 구분하고 조건문에서 판별하기 위한 규칙 이름 (식별자) | "length", "number" , "repetition"|
| 1번 열 | 해당 조건을 만족했을 때 부여하거나 차감할 기준 점수 (가산점/감점) | 3, 2, -3 |
| 2번 열 | 사용자 화면에 검사 항목으로 출력할 규칙에 대한 설명 메시지 | 비밀번호 길이가 8자리 이상인가? |
| 3번 열 | (선택 사항) 해당 규칙을 통과하지 못하면 무조건 '위험'으로 처리할지 결정하는 필수 여부 (True/False) | "TRUE", "FALSE" |

이 2차원 리스트를 사용한 이유: 만약 2차원 리스트를 사용하지 않았다면 각 규칙의 이름, 점수, 설명 문구를 저장하기 위해 수많은 개별 변수를 선언해야 해서 코드가 복잡해졌을 것입니다. 연관된 데이터를 행과 열의 구조로 묶어 관리함으로써, 규칙이 늘어나더라도 for 반복문 하나만으로 모든 규칙을 순서대로 일괄 처리할 수 있어 프로그램의 확장성과 가독성이 크게 향상되기 때문입니다.


---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| get_password | 사용자에게 주의사항(개인정보 보호 등)을 안내하고, 안전하게 비밀번호를 입력받습니다. | 없음 | 사용자가 입력한 비밀번호 (문자열) |
| check_length | 입력된 비밀번호의 길이가 8자 이상인지 판단한다. | password (문자열) | 만족 여부 (True/False) |
| evaluate_password | 2차원 리스트 규칙을 돌며 전체 점수를 계산하고 합산한다. | password (문자열), criteria (2차원 리스트) | 최종 계산된 총점 (정수) |
| print_result | 최종 점수에 따라 안전 등급을 판정하고 결과를 화면에 예쁘게 보여준다. | total_score (정수) | 없음 (화면에 결과 바로 출력) |

가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름: evaluate_password

설명: 이 함수는 매개변수로 전달받은 2차원 리스트 criteria를 반복문으로 순회하면서, 각 규칙의 이름(rule_name), 점수(score), 설명(description)을 인덱싱으로 추출합니다. 그 후 다중 조건문(if-elif)을 통해 규칙명이 무엇인지 판단하고, 비밀번호의 길이를 측정하거나 내부에 포함된 숫자의 개수를 세는 로직을 수행합니다. 조건을 만족하면 total_score에 가산점을 더하고, 실패 시에는 원인 리스트(failed_reasons)에 내용을 추가합니다. 특히 감점 규칙의 경우 음수 값을 더해 자동으로 점수가 깎이도록 설계되었으며, 최종적으로 누적된 총점만을 반환하는 프로그램의 핵심 제어 장치입니다.


---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

if has_space == 1: 구문을 통해 비밀번호 중간에 공백이 포함되어 있는지 유효성을 검사했습니다.

if rule_name == "length": 등 2차원 리스트에서 꺼낸 규칙명이 무엇인지 검사하여 실행할 로직을 분기했습니다.

if len(user_password) >= 8:을 통해 글자 수가 기준치 이상인지, if number_count > 0:을 통해 숫자가 하나라도 존재하는지 판단했습니다.

if score >= 4: 구문으로 최종 점수에 따른 안전 등급(안전/보통/위험)을 판별했습니다.

전반적으로 특정 값에 따라 출력 방법을 달리하게 사용했습니다.


### 반복문 사용 부분

for char in user_input:을 통해 사용자가 입력한 문자열을 한 글자씩 꺼내어 공백 여부와 숫자의 개수를 검사하는 과정을 반복했습니다.

for rule in criteria:를 통해 2차원 리스트에 저장된 3가지 보안 규칙 데이터를 처음부터 끝까지 한 행씩 행 단위로 꺼내는 과정을 반복했습니다.

for i in range(len(user_password) - 2):를 통해 문자열의 인덱스를 활용해 3연속 동일 문자가 존재하는지 구간별로 비교하는 과정을 반복했습니다.


### 조건문과 반복문이 함께 작동한 부분

'3연속 동일 문자 반복 검사' 부분과 '숫자 개수 세기' 부분에서 두 문법이 결합하여 강력하게 작동했습니다.
숫자 검사에서는 for문으로 비밀번호를 한 글자씩 반복하면서, 동시에 if char in number_characters:라는 조건문을 실행해 해당 글자가 숫자가 맞는지 끊임없이 판별했습니다.


---

## 6. 실행 결과

대표 실행 결과를 붙여넣으세요.

====== 회원가입 비밀번호 안전성 진단 프로그램 ======
※ 주의: 실제 사용 중인 개인 비밀번호는 입력하지 마세요.
검사할 가상의 비밀번호를 입력하세요: anyoneslegendFlandre27

--- 안전성 검사 시작 ---
[성공] 비밀번호 길이가 8자리 이상인가? (+3점)
[성공] 숫자가 포함되어 있는가? (+2점)
[안전] 연속된 반복 문자가 없습니다. (0점)

==============================
최종 안전성 점수: 5점
최종 등급: [안전] 사용 가능한 비밀번호입니다.
==============================

Q1. 추천 비밀번호를 생성해 드릴까요? (Y/N): Y

--- 맞춤형 추천 비밀번호 ---
기존 비밀번호를 보완한 추천 비밀번호: anyoneslegendFlandre272026!@
TIP: 연속된 문자는 피하고, 대소문자와 숫자를 골고루 섞어주세요.

결과 분석: 비밀번호가 8자 이상에, 숫자 24가 포함되고, 반복되는 글자가 없으므로, 고득점이 나왔고, 뒤어 숫자, 기호를 포함한 추천 비밀번호를 출력했다고 할 수 있습니다.


---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

입력: anyoneslegendShanks24, Y

최종 안전성 점수: 5점
최종 등급: [안전] 사용 가능한 비밀번호입니다. 

그리고 Y 선택시 기존 비밀번호를 보완한 추천 비밀번호: anyoneslegendShanks242026!@

실제 결과: --- 안전성 검사 시작 ---
[성공] 비밀번호 길이가 8자리 이상인가? (+3점)
[성공] 숫자가 포함되어 있는가? (+2점)
[안전] 연속된 반복 문자가 없습니다. (0점)

==============================
최종 안전성 점수: 5점
최종 등급: [안전] 사용 가능한 비밀번호입니다.
==============================

Y 선택 후: --- 맞춤형 추천 비밀번호 ---
기존 비밀번호를 보완한 추천 비밀번호: anyoneslegendShanks242026!@
TIP: 연속된 문자는 피하고, 대소문자와 숫자를 골고루 섞어주세요.


### 테스트 2: 예외 또는 경계 사례

입력: yagao go go

예상 결과: 띄어쓰기를 포함 할 수 없다며 프로그램 종로

실제 결과: [오류] 비밀번호 중간에 공백(띄어쓰기)을 포함할 수 없습니다.
유효하지 않은 입력이므로 프로그램을 종료합니다.


### 발견한 오류와 수정한 방법


---

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분: 2차원 리스트를 어떤 함수와, 요소로 구성할지에 대해 묻고, 이해를 위하여 그 이차원 리스트의 원리를 물었다. 또한 마지막에 평가기준 만족 여부를 물었다.


Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분: 원래 반복 부분이 존재하는 지는 Gemini가 True, False 구문으로 하자고 했지만, 더 쉽게 0과 1로 구분하도록 수정했다.


최종 코드에서 내가 직접 판단하거나 수정한 부분: 최종 점수를 볼지 묻는 질문의 필요성이 적어 삭제했습니다.


---

## 9. 한계와 개선 방안

현재 프로그램의 한계: 반복 비밀번호에 관련하여 감점을 하기로 베이스를 짰지만, 오히려 문제가 되지 않는 반복(영문 비밀번호 사이에 잠깐 넣기 등)
에도 감점이 들어간다는 단점이 존재한다.


다시 개선한다면 추가하고 싶은 기능:비밀번호 111111 이런 식의 반복이냐 아니면 Leaper2'000'처럼 짧지만 너무 길지 않은 우연 같은 반복인지를 구분하는 기능을 넣고 싶다.


---

## 10. 코드인터뷰 준비

다음 질문에 답할 수 있도록 준비하세요.

1. 내 프로그램의 입력-처리-출력은 무엇인가?
2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
4. 조건문과 반복문은 어디에서 사용되었는가?
5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?
